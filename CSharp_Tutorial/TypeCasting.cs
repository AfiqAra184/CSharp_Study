using System;
using System.Drawing;

namespace CSharp_Tutorial
{
    internal class TypeCasting
    {
        //static void Main(string[] args)
        //{
        //    // Type casting is when you assign a value of one data type to another type
        //    // Implicit Casting (automatically) - converting a smaller type to a larger type size
        //    // char -> int -> long -> float -> double
        //    // Explicit Casting(manually) -converting a larger type to a smaller size type
        //    // double -> float -> long -> int -> char

        //    //implicit casting
        //    int myInt = 9;
        //    double myDouble = myInt; //Automatic casting: int to double

        //    Console.WriteLine("myInt is " + myInt);
        //    Console.WriteLine("Implicit casting myDouble = myInt is " + myDouble);

        //    //explicit casting
        //    myDouble = 9.78;
        //    myInt = (int)myDouble; //Manual casting: double to int

        //    Console.WriteLine("myDouble is " + myDouble);
        //    Console.WriteLine("Explicit casting myInt = (int)myDouble is " + myInt);

        //    //Type Conversion Methods
        //    //It is also possible to convert data types explicitly by using built-in methods,
        //    //such asConvert.ToBoolean, Convert.ToDouble, Convert.ToString, Convert.ToInt32 (int)
        //    //and Convert.ToInt64 (long):

        //    myInt = 10;
        //    myDouble = 5.25;
        //    bool myBool = true;

        //    Console.WriteLine("myInt = 10; myDouble = 5.25; myBool = true");

        //    Console.WriteLine("Convert.ToString(myInt) is " + Convert.ToString(myInt)); //convert int to string
        //    Console.WriteLine("Convert.ToDouble(myInt) is " + Convert.ToDouble(myInt)); //convert int to double
        //    Console.WriteLine("Convert.ToInt32(myDouble) is " + Convert.ToInt32(myDouble)); //convert double to int
        //    Console.WriteLine("Convert.ToString(myBool) is " + Convert.ToString(myBool)); //convert bool to string
        //}
    }
}
